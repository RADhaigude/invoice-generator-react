name: CI / SonarCloud Scan

on:
  push:
    branches: [ dev, master ]
  pull_request:
    branches: [ master ]

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      # 1️⃣ Checkout repository (fix shallow clone & ref)
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 2️⃣ Tooling (Node + jq)
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install jq
        run: |
          sudo apt-get update
          sudo apt-get install -y jq curl

      # 3️⃣ Cache Node modules
      - name: Cache Node modules
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      # 4️⃣ Install dependencies
      - name: Install dependencies
        run: npm ci

      # 5️⃣ Run tests with coverage
      - name: Run tests with coverage
        run: |
          npm install --save-dev jest-canvas-mock
          echo "import 'jest-canvas-mock';" > src/setupTests.js
          npx jest --coverage --watchAll=false --passWithNoTests --bail=0 \
            --json --outputFile=jest-results.json \
            --coverageReporters=text --coverageReporters=lcov --coverageReporters=html || echo "⚠️ Some tests failed, but continuing..."

      # 6️⃣ Build project
      - name: Build project
        run: CI=false npm run build --if-present
        env:
          NODE_OPTIONS: --openssl-legacy-provider

      # 7️⃣ SonarCloud Scan
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=RADhaigude_invoice-generator-react
            -Dsonar.organization=radhaigude
            -Dsonar.sources=.
            -Dsonar.exclusions=node_modules/**,coverage/**,build/**,dist/**,**/*.spec.ts,**/*.spec.tsx,**/*.test.ts,**/*.test.tsx,**/*.test.js
            -Dsonar.javascript.lcov.reportPaths=coverage/lcov.info
            -Dsonar.python.version=3.11   # ✅ precise python version

      # 8️⃣ Wait for SonarCloud Quality Gate
      - name: Wait for SonarCloud Quality Gate
        id: quality_gate
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          echo "⏳ Waiting for SonarCloud analysis to complete..."
          for i in {1..10}; do
            curl -s -u "$SONAR_TOKEN:" \
              "https://sonarcloud.io/api/qualitygates/project_status?projectKey=RADhaigude_invoice-generator-react" \
              -o sonar_quality_gate.json || true
            if grep -q '"status"' sonar_quality_gate.json; then
              STATUS=$(jq -r '.projectStatus.status' sonar_quality_gate.json)
              echo "✅ Quality Gate JSON received: $STATUS"
              echo "status=$STATUS" >> $GITHUB_OUTPUT
              break
            else
              echo "⚠️ Not ready yet... retrying in 15s"
              sleep 15
            fi
          done

      # 9️⃣ Fetch Sonar metrics (bugs, vulns, smells, coverage, duplication)
      - name: Fetch Sonar Metrics
        id: sonar_metrics
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          curl -s -u "$SONAR_TOKEN:" \
            "https://sonarcloud.io/api/measures/component?component=RADhaigude_invoice-generator-react&metricKeys=bugs,vulnerabilities,code_smells,coverage,duplicated_lines_density" \
            -o sonar_metrics.json
          BUGS=$(jq -r '.component.measures[] | select(.metric=="bugs") | .value' sonar_metrics.json)
          VULNS=$(jq -r '.component.measures[] | select(.metric=="vulnerabilities") | .value' sonar_metrics.json)
          SMELLS=$(jq -r '.component.measures[] | select(.metric=="code_smells") | .value' sonar_metrics.json)
          COVERAGE=$(jq -r '.component.measures[] | select(.metric=="coverage") | .value' sonar_metrics.json)
          DUP=$(jq -r '.component.measures[] | select(.metric=="duplicated_lines_density") | .value' sonar_metrics.json)

          echo "bugs=$BUGS" >> $GITHUB_OUTPUT
          echo "vulns=$VULNS" >> $GITHUB_OUTPUT
          echo "smells=$SMELLS" >> $GITHUB_OUTPUT
          echo "coverage=$COVERAGE" >> $GITHUB_OUTPUT
          echo "dup=$DUP" >> $GITHUB_OUTPUT

      # 🔟 Notify Teams with MessageCard
      - name: Notify Teams
        if: always()
        env:
          WEBHOOK_URL: ${{ secrets.TEAMS_WEBHOOK_URL }}
        run: |
          STATUS="${{ steps.quality_gate.outputs.status }}"
          BUGS="${{ steps.sonar_metrics.outputs.bugs }}"
          VULNS="${{ steps.sonar_metrics.outputs.vulns }}"
          SMELLS="${{ steps.sonar_metrics.outputs.smells }}"
          COVERAGE="${{ steps.sonar_metrics.outputs.coverage }}"
          DUP="${{ steps.sonar_metrics.outputs.dup }}"

          if [ "$STATUS" = "OK" ]; then
            RESULT="✅ PASSED"
          else
            RESULT="❌ FAILED"
          fi

          if [ "${{ github.event_name }}" = "pull_request" ]; then
            TARGET="Pull Request #${{ github.event.pull_request.number }}"
          else
            TARGET="Branch ${GITHUB_REF_NAME}"
          fi

          payload=$(jq -n \
            --arg title "🚦 SonarCloud CI/CD Quality Gate Report" \
            --arg target "$TARGET" \
            --arg result "$RESULT" \
            --arg bugs "$BUGS" \
            --arg vulns "$VULNS" \
            --arg smells "$SMELLS" \
            --arg coverage "$COVERAGE%" \
            --arg dup "$DUP%" \
            --arg dash "https://sonarcloud.io/project/overview?id=RADhaigude_invoice-generator-react" \
            '{
              "@type": "MessageCard",
              "@context": "http://schema.org/extensions",
              "themeColor": "0076D7",
              "summary": "SonarCloud Quality Gate Report",
              "sections": [{
                "activityTitle": $title,
                "activitySubtitle": "Target: \($target)",
                "facts": [
                  {"name": "Quality Gate", "value": $result},
                  {"name": "Bugs", "value": $bugs},
                  {"name": "Vulnerabilities", "value": $vulns},
                  {"name": "Code Smells", "value": $smells},
                  {"name": "Coverage", "value": $coverage},
                  {"name": "Duplication", "value": $dup}
                ],
                "markdown": true
              }],
              "potentialAction": [{
                "@type": "OpenUri",
                "name": "🔗 View Dashboard",
                "targets": [{ "os": "default", "uri": $dash }]
              }]
            }')

          echo "Sending MessageCard to Teams..."
          curl -H "Content-Type: application/json" -d "$payload" "$WEBHOOK_URL"
